.list-group
	= link_to posts_path, class: "list-group-item" do
		%span.badge= Post.all.count
		Movie
	= link_to siris_path, class: "list-group-item #{current_controller?(['siris'])}" do
		%span.badge= Siri.all.count
		TV Series
	= link_to users_path, class: "list-group-item" do
		%span.badge= User.all.count
		User
	= link_to "Comment", "#", class: "list-group-item"
	= link_to genres_path, class: "list-group-item" do
		%span.badge= Genre.all.count
		Movie Genre
	= link_to siri_genres_path, class: "list-group-item" do
		%span.badge= SiriGenre.all.count
		TV Series Genre
- content_for :title do
	KakiTube | Post List
- content_for :meta_tags do
	= render :partial => 'application/meta_tags', :locals => { :type => "website", :title => "KakiTube | Post List", :url => request.url, :description => "KakiTube", :canonical => request.url, :og_title => "KakiTube | Post List", :og_description => "KakiTube" }
	
/ Page Content
.container
	.row
		.col-lg-12
			.page-header
				%h1 Genre List
	.row
		.col-md-4
			= render partial: "sidebar"
		.col-md-8
			.panel.panel-default
				.panel-heading
					%h3.panel-title
						Genre List
				.panel-body
					= link_to "New Genre", new_genre_path, id: "new_type", remote: true
					%hr
					%table.table.table-bordered.table-hovered
						%thead
							%tr
								%th Name
								%th.col-md-2 Date Added
								%th.col-md-2 Action
						%tbody
							- @genres.each do |genre|
								%tr{id: "all_genre"}
								%tr
									%td= genre.name
									%td= genre.created_at.strftime("%d %B %Y")
									%td= link_to "(remove)", genre, method: :delete, data: {confirm: "Are you sure?"}
				/ .panel-footer

